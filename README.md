# IMDb Top Movies Scraper 🎥

This Python script uses Selenium to scrape top movies from IMDb and saves them into a JSON file.

## Features
- Scrapes movie title & release year
- Configurable number of movies
- Saves data into structured JSON

## Usage
```bash
pip install -r requirements.txt
python scraper.py
